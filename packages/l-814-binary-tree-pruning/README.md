# l-814-binary-tree-pruning

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test l-814-binary-tree-pruning` to execute the unit tests via [Jest](https://jestjs.io).
